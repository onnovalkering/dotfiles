#!/usr/bin/env sh

alias redis-cli='docker run --rm -it --network host redis:alpine redis-cli'

alias lofar-proxy-init='docker run --rm -it -u "$(id -u):$(id -g)" -v ~/.globus:/globus onnovalkering/proxy-init -voms lofar:/lofar/user'